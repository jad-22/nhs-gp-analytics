"""Clustering helpers for the NHS GP Analytics project scaffold."""

from __future__ import annotations

import pandas as pd


def cluster_practices(df: pd.DataFrame, n_clusters: int = 6) -> pd.DataFrame:
    """Placeholder for clustering implementation."""

    raise NotImplementedError("Clustering will be implemented in the next pass.")


def umap_embed(df: pd.DataFrame) -> pd.DataFrame:
    """Placeholder for 2D UMAP embedding."""

    raise NotImplementedError("UMAP embedding will be implemented in the next pass.")